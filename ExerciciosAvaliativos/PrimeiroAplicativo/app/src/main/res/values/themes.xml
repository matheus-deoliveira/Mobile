<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.PrimeiroAplicativo" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    
    <style name="EditTextFieldTop">
        <item name="android:layout_marginTop">40dp</item>
        <item name="android:layout_marginStart">10dp</item>
        <item name="android:layout_marginEnd">10dp</item>
    </style>

    <style name="EditTextFieldMiddle">
        <item name="android:layout_marginTop">40dp</item>
        <item name="android:layout_marginStart">10dp</item>
        <item name="android:layout_marginEnd">10dp</item>
    </style>

    <style name="TextViewFieldInfo">
        <item name="android:textColor">@color/grey</item>
    </style>

    <style name="ImpostoTextView">
        <item name="android:textSize">30sp</item>
        <item name="android:textColor">@color/blue</item>
        <item name="android:layout_marginTop">15dp</item>
    </style>

    <style name="Button">
        <item name="android:backgroundTint">@color/green</item>
        <item name="android:layout_marginTop">20dp</item>
        <item name="android:layout_marginStart">5dp</item>
        <item name="android:layout_marginEnd">5dp</item>
    </style>

    <style name="Theme.PrimeiroAplicativo" parent="Base.Theme.PrimeiroAplicativo" />
</resources>